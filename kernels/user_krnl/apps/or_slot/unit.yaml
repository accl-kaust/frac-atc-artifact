name: or_slot
moduletype: recon
top: or_slot
build: synth
build_dir: build
version: 0.1.0
part: xcu280-fsvh2892-2L-e
arch: ultraplus

rtl:
  dir: "src/rtl"
  files:
    - or_slot.v

xdc:
  dir: "src/xdc"
  files:

xci:
  dir: "src/xci"
  files:

ip:
  dir: "src/ip"
  files:
